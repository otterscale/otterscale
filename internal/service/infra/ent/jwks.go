// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/openhdc/openhdc/internal/service/infra/ent/jwks"
)

// JWKS is the model entity for the JWKS schema.
type JWKS struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// PublicKey holds the value of the "public_key" field.
	PublicKey string `json:"public_key,omitempty"`
	// PrivateKey holds the value of the "private_key" field.
	PrivateKey string `json:"private_key,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JWKS) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case jwks.FieldID, jwks.FieldPublicKey, jwks.FieldPrivateKey:
			values[i] = new(sql.NullString)
		case jwks.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JWKS fields.
func (j *JWKS) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jwks.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				j.ID = value.String
			}
		case jwks.FieldPublicKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field public_key", values[i])
			} else if value.Valid {
				j.PublicKey = value.String
			}
		case jwks.FieldPrivateKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field private_key", values[i])
			} else if value.Valid {
				j.PrivateKey = value.String
			}
		case jwks.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				j.CreatedAt = value.Time
			}
		default:
			j.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JWKS.
// This includes values selected through modifiers, order, etc.
func (j *JWKS) Value(name string) (ent.Value, error) {
	return j.selectValues.Get(name)
}

// Update returns a builder for updating this JWKS.
// Note that you need to call JWKS.Unwrap() before calling this method if this JWKS
// was returned from a transaction, and the transaction was committed or rolled back.
func (j *JWKS) Update() *JWKSUpdateOne {
	return NewJWKSClient(j.config).UpdateOne(j)
}

// Unwrap unwraps the JWKS entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (j *JWKS) Unwrap() *JWKS {
	_tx, ok := j.config.driver.(*txDriver)
	if !ok {
		panic("ent: JWKS is not a transactional entity")
	}
	j.config.driver = _tx.drv
	return j
}

// String implements the fmt.Stringer.
func (j *JWKS) String() string {
	var builder strings.Builder
	builder.WriteString("JWKS(")
	builder.WriteString(fmt.Sprintf("id=%v, ", j.ID))
	builder.WriteString("public_key=")
	builder.WriteString(j.PublicKey)
	builder.WriteString(", ")
	builder.WriteString("private_key=")
	builder.WriteString(j.PrivateKey)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(j.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JWKSs is a parsable slice of JWKS.
type JWKSs []*JWKS
